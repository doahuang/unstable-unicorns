const Downgrade = require('../../types/downgrade');

const BlindingLight = new Downgrade(
  'Blinding Light',
  'All of your Unicorn cards are considered Basic Unicorns with no effects.'
);

module.exports = BlindingLight;
