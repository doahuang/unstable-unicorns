const Downgrade = require('../../types/downgrade');

const TinyStable = new Downgrade(
  'Tiny Stable',
  'If at any time you have more than 5 Unicorns in your Stable, SACRIFICE a Unicorn card.'
);

module.exports = TinyStable;
