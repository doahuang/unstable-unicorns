const Upgrade = require('../../types/upgrade');

const GlitterBomb = new Upgrade(
  'Glitter Bomb',
  'If this card is in your Stable at the beginning of your turn, you may SACRIFICE a card, then DESTROY a card.'
);

module.exports = GlitterBomb;
