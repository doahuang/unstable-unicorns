const Magic = require('../../types/magic');

const ReTarget = new Magic(
  'Re-Target',
  "Move an Upgrade or Downgrade card from any player's Stable to any other player's Stable."
);

module.exports = ReTarget;
