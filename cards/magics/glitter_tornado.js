const Magic = require('../../types/magic');

const GlitterTornado = new Magic(
  'Glitter Tornado',
  "Return a card in each player's Stable (including yours) to their hand."
);

module.exports = GlitterTornado;
